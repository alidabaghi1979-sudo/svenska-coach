# Svenska Daily Lesson — dependencies (all light, GitHub Actions friendly)
requests>=2.31
pydantic>=2.6
python-dotenv>=1.0
mutagen>=1.47        # ID3 tags for MP3
genanki>=0.13        # Anki .apkg export
gspread>=6.0         # Svenska Coach Google Sheet
google-auth>=2.20
tomli>=2.0; python_version < "3.11"   # read ../.streamlit/secrets.toml
tzdata; sys_platform == "win32"      # timezone data for Windows
